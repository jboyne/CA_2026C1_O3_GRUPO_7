# Integrantes



# Comandos

```bash
cat var.tar.gz.part-* > var.tar.gz
tar -xzf var.tar.gz -C /var/

tar -xzf root.tar.gz -C /root/
tar -xzf etc.tar.gz -C /etc/
tar -xzf opt.tar.gz -C /opt/
tar -xzf www_dir.tar.gz -C /www_dir/
tar -xzf backup_dir.tar.gz -C /backup_dir/

rm -rf var
rm -rf root
rm -rf etc
rm -rf opt
rm -rf www_dir
rm -rf backup_dir
```